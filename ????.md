# 罗汉到家上门按摩 MVP

## 项目简介

罗汉到家是一个纯前端的上门按摩预约 MVP，用于演示从选择技师、选择服务、预约时间、创建订单到查看订单进度的完整流程。

项目不依赖后端、数据库、登录、支付或需要 Key 的地图服务，所有订单数据保存在浏览器 LocalStorage 中。

## 功能清单

- 技师列表：按距离展示附近技师，支持关键词搜索。
- 地图展示：使用 Leaflet 与 OpenStreetMap 显示长沙附近技师位置；点击 Marker 可查看技师信息。
- 服务预约：选择技师、服务项目、未来三天日期与服务时段。
- 订单任务：下单后展示创建订单、匹配技师、等待接单的模拟任务进度。
- 订单详情：通过订单 ID 查看技师、服务、预约时间、金额与状态。
- 历史订单：展示全部订单，并按创建时间倒序排列。
- 订单状态：支持“待接单 → 已接单 → 服务中 → 已完成”的手动推进，刷新后仍保留。

## 本地运行

```bash
pnpm install
pnpm run dev
```

打开终端输出的地址，通常为 `http://localhost:5173`。

如未安装 pnpm，也可使用：

```bash
npm install
npm run dev
```

## 主要页面

| 页面 | 路径 | 用途 |
| --- | --- | --- |
| 技师列表 | `/` | 查看长沙附近技师、地图与订单入口 |
| 技师详情 | `/technician/:id` | 选择服务项目 |
| 确认预约 | `/booking` | 选择日期和时段并创建订单 |
| 订单任务 | `/order-status/:id` | 展示订单创建进度 |
| 订单详情 | `/order/:id` | 查看订单并推进状态 |
| 我的订单 | `/orders` | 查看全部历史订单 |

## 订单存储

订单业务统一由 `src/services/orderService.js` 管理，使用的 LocalStorage Key 为：

```text
luohan_orders
```

每次确认预约都会创建并追加一个新订单，不会覆盖已有订单。

## 技术栈

- React
- Vite
- React Router
- Leaflet
- OpenStreetMap
- CSS
- LocalStorage

## 注意事项

- 地图使用公开 OpenStreetMap 瓦片，不需要申请地图 Key。
- 地图加载失败时会显示降级提示，不影响技师列表、预约和订单功能。
- 当前项目定位为 MVP，暂不包含真实登录、支付、定位和后端接口。
