# 罗汉到家上门按摩 MVP

## 项目简介

罗汉到家是一个轻量上门按摩预约 MVP，用于演示用户从选择技师、选择服务、预约时间，到创建与管理订单的完整流程。

## 页面

| 页面 | 路径 | 用途 |
| --- | --- | --- |
| 技师列表 | `/` | 查看长沙附近技师、地图与订单入口 |
| 技师详情 | `/technician/:id` | 查看服务项目并选择服务 |
| 确认预约 | `/booking` | 选择日期、时间并创建订单 |
| 订单任务 | `/order-status/:id` | 展示下单后的任务进度 |
| 订单详情 | `/order/:id` | 查看订单并推进状态 |
| 我的订单 | `/orders` | 查看历史订单 |

## 数据与后端

后端目录保持 MVP 的最小结构：

```text
backend/
├── main.py          # FastAPI 接口
├── database.py      # SQLite 连接
├── models.py        # 两张数据表模型
└── requirements.txt
```

数据库仅包含两张表：

- `technicians`：技师 ID、姓名、评分、距离、起步价格
- `orders`：技师 ID、服务、价格、预约日期时间、状态、创建时间

订单不再使用 LocalStorage。确认预约、查询历史订单、推进订单状态都会调用本地 FastAPI，再写入 SQLite。

## 运行

后端：

```powershell
cd backend
python -m pip install -r requirements.txt
python -m uvicorn main:app --port 8100
```

前端：

```powershell
pnpm install
pnpm run dev
```

前端固定请求 `http://127.0.0.1:8100`，因此开发时需保持两个服务同时运行。
